/**
 * Every word in the hero lives here, so copy can change without touching JSX.
 * Numbers must match the rest of the page. Confirm them before launch.
 */
export const hero = {
  badge: 'We guide you end to end till placement.',

  // Split so the second line can carry more weight than the first.
  headlineLight: 'Learn analytics in 60 days.',
  headlineBold: 'Pay 50% of your course fee only after you ',
  // Only these words get the gold stroke that draws itself in.
  headlineAccent: 'get the job.',

  // The header echoes the section's main action.
  primaryCta: { label: 'Start your job training', href: '#enroll' },

}
